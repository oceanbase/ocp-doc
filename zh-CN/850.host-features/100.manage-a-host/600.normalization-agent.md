# 检查主机标准化

数据库集群作为数据的基石，性能及稳定性对生产环境极为重要，为了确保数据库集群能发挥最大的价值，建议上线前对主机环境进行标准化检查，并对其中可能存在的风险进行修复。

本节为您介绍在 OCP 上添加主机后，执行主机标准化检查的操作步骤。

## 前提条件

* 请确保当前登录 OCP 的用户至少具备如下权限：

  * **主机维护** 资源权限。
  * 主机 **<UI-TERM key="ocp-v2.component.HostSelect.All">全部</UI-TERM>** 菜单权限。

* 已 [添加主机](200.add-a-host.md)。

## 操作步骤

1. 登录 OCP。

2. 在左侧导航栏中单击 **<UI-TERM key="ocp-v2.Service.Component.ServiceModal.Host">主机</UI-TERM>**。

3. 进入主机列表页面，根据实际业务场景，找到主机标准化的入口。

   * 当新主机处于 **<UI-TERM key="ocp-v2.src.constant.compute.Idle">空闲</UI-TERM>** 状态，且已知晓或已完成主机标准化时，您可单击操作列的 **...**，选择 **<UI-TERM key="ocp-v2.page.Host.HostStandardization">主机标准化</UI-TERM>**。

   * 当新主机处于 **<UI-TERM key="ocp-v2.src.constant.oceanbase.Idle">空闲</UI-TERM>** 状态且未进行标准化操作时，将光标悬停于主机 ![1](https://obbusiness-private.oss-cn-shanghai.aliyuncs.com/doc/img/ocp/410/%E7%A9%BA%E9%97%B2%E5%9B%BE%E6%A0%87.png) 图标上，在弹出的提示中单击 **<UI-TERM key="ocp-v2.page.Host.HostStandardization">主机标准化</UI-TERM>** 。

4. 在对话框中选择主机标准化的执行操作类型及产品类型，单击 **确定**。

   |  参数  |  说明 |
   |--------|-------|
   | 执行操作  | 分为 **<UI-TERM key="ocp-v2.page.Host.CheckAndRepairAutomatically">检查并自动修复</UI-TERM>** 和 **<UI-TERM key="ocp-v2.page.Host.CheckOnly">仅检查</UI-TERM>** 两种类型。<ul><li>检查并自动修复：系统将对主机进行检查，并自动修复检查过程中所发现的风险。</li><li>仅检查：系统将只对主机进行检查操作，而不会自动修复检查过程中发现的风险。</li></ul>   |
   | 产品类型  | 分为 **OceanBase** 和 **OBProxy** 两种类型，支持多选。系统将针对不同的产品类型，执行其依赖的主机标准化检查项。  |

5. 检查任务结束后，若主机中存在风险，您可参考如下步骤进行修复：

   * 当主机标准化的执行操作为 **检查并自动修复** 时：

     1. 单击 **查看修复报告** 查看详细的风险明细，并根据报告中的信息手动修复风险。
     2. 待风险修复后，可单击 **重新检查**，重新对主机发起一个检查任务。

   * 当主机标准化的执行操作为 **仅检查** 时：

     1. 单击 **查看检查报告** 查看详细的风险明细。
     2. 若检查报告中有可自动修复的风险，可单击 **自动修复** 按钮，系统将对风险项进行自动修复；对于检查报告中不可自动修复的风险，您需根据报告中的信息进行手动修复。
     3. 待风险修复后，可单击 **重新检查** ，重新对主机发起一个检查任务。

## 相关文档

关于主机执行的标准化检查项，可参见 [主机标准化检查项](700.host-standardized-inspection-scope.md)。
