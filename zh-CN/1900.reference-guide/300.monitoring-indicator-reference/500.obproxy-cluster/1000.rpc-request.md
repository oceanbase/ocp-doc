# RPC 请求数

本节为您介绍 OBProxy 的监控指标 **RPC 请求数**。

## 指标介绍

OBProxy RPC 请求数量。

## 指标参数说明

|    **<UI-TERM key="ocp-v2.Monitor.AddMonitor.IndicatorItem">指标项</UI-TERM>**     |          **指标名称**           | **<UI-TERM key="ocp-v2.src.template.Table.Unit">单位</UI-TERM>** |
|----------------|-----------------------------|--------|
| rpc_request_count | odp_rpc_request_count | 次   |

## 计算表达式

sum(rate(odp_rpc_request_total{@LABELS}[@INTERVAL])) by (@GBLABELS)
