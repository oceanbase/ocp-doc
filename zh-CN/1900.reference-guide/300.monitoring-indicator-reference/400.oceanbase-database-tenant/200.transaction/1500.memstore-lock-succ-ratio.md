# Memstore 锁申请成功率

本节为您介绍 OceanBase 租户的性能指标 **Memstore 锁申请成功率** 。 **Memstore 锁申请成功率** 指标用于统计 OceanBase 租户的 Memstore 锁申请成功率。该指标细分为 2 个子指标对租户的 XA 语句耗时进行统计，包括：memstore_read_lock_succ_ratio、memstore_write_lock_succ_ratio。您可指定统计范围，查询租户在单个 Zone 或 OBServer 节点的 **Memstore 锁申请成功率** 。

## memstore_read_lock_succ_ratio

### 指标介绍

OceanBase 租户 Memstore 读锁申请成功率。

### 指标参数说明

|   指标项   |   指标名称   |  单位  |
|-----------|----------|--------|
| memstore_read_lock_succ_ratio | memstore_read_lock_succ_ratio | %  |

### 计算表达式

100 * 1 / (1 + sum(rate(ob_sysstat{stat_id="60020",@LABELS}[@INTERVAL])) by (@GBLABELS) / sum(rate(ob_sysstat{stat_id="60019",@LABELS}[@INTERVAL])) by (@GBLABELS))

### 采集 SQL

* OceanBase V2.2.70 以上、V4.0 以下版本：

  ```sql
  select /*+ MONITOR_AGENT READ_CONSISTENCY(WEAK) */ con_id tenant_id, stat_id, value from v$sysstat where stat_id IN (60019, 60020) and (con_id > 1000 or con_id = 1) and class < 1000
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  select /* MONITOR_AGENT */ con_id tenant_id, stat_id, value from v$sysstat where stat_id IN (60019, 60020) and (con_id > 1000 or con_id = 1) and class < 1000
  ```

## memstore_write_lock_succ_ratio

### 指标介绍

OceanBase 租户 Memstore 写锁申请成功率。

### 指标参数说明

|  指标项   |    指标名称    |   单位  |
|----------|---------|--------|
| memstore_write_lock_succ_ratio | memstore_write_lock_succ_ratio | %      |

### 计算表达式

100 * 1 / (1 + sum(rate(ob_sysstat{stat_id="60022",@LABELS}[@INTERVAL])) by (@GBLABELS) / sum(rate(ob_sysstat{stat_id="60021",@LABELS}[@INTERVAL])) by (@GBLABELS))'

### 采集 SQL

* OceanBase V2.2.70 以上、V4.0 以下版本：

  ```sql
  select /*+ MONITOR_AGENT READ_CONSISTENCY(WEAK) */ con_id tenant_id, stat_id, value from v$sysstat where stat_id IN (60021, 60022) and (con_id > 1000 or con_id = 1) and class < 1000
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  select /* MONITOR_AGENT */ con_id tenant_id, stat_id, value from v$sysstat where stat_id IN (60021, 60022) and (con_id > 1000 or con_id = 1) and class < 1000
  ```
