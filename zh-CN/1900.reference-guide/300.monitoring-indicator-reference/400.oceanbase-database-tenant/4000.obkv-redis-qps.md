# QPS

本节为您介绍租户 OBKV_Redis 类型监控项的性能指标 **QPS**，该指标统计了租户平均每秒处理的 SQL 行数。您可指定统计范围，查询租户在单个 Zone 或 OBServer 节点的 OBKV_Redis 类型的 **QPS** 数据。

## all

### 指标介绍

每秒处理 SQL 语句数。

### 指标参数说明

| **指标项** |   **指标名称**    | **单位** |
|---------|---------------|--------|
| all     | obkv_redis_sql_all_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191643",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191631",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191589",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191595",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191509",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191517",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191541",@LABELS}[@INTERVAL])) by (@GBLABELS) + sum(rate(ob_sysstat{stat_id="191557",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## set

### 指标介绍

每秒处理 SET 语句数。

### 指标参数说明

| **指标项** |     **指标名称**     | **单位** |
|---------|------------------|--------|
| set  | obkv_redis_sql_set_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191643",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
   
  ```

## get

### 指标介绍

每秒处理 GET 语句数。

### 指标参数说明

| **指标项** |     **指标名称**     | **单位** |
|---------|------------------|--------|
| get  | obkv_redis_sql_get_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191631",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## hset

### 指标介绍

每秒处理 HSET 语句数。

### 指标参数说明

| **指标项** |     **指标名称**     | **单位** |
|---------|------------------|--------|
| hset  | obkv_redis_sql_hset_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191589",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## hget

### 指标介绍

每秒处理 HGET 语句数。

### 指标参数说明

| **指标项** |     **指标名称**     | **单位** |
|---------|------------------|--------|
| hget  | obkv_redis_sql_hget_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191595",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## lpush

### 指标介绍

每秒处理 LPUSH 语句数。

### 指标参数说明

| **指标项** |     **指标名称**      | **单位** |
|---------|-------------------|--------|
| lpush | obkv_redis_sql_lpush_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191509",@LABELS}[@INTERVAL])) by (@GBLABELS)

### SQL 采集

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## lpop

### 指标介绍

每秒处理 LPOP 语句数。

### 指标参数说明

| **指标项** |    **指标名称**     | **单位** |
|---------|-----------------|--------|
| lpop   | obkv_redis_sql_lpop_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191517",@LABELS}[@INTERVAL])) by (@GBLABELS)

### 采集 SQL

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## sadd

### 指标介绍

每秒处理 SADD 语句数。

### 指标参数说明

| **指标项** |    **指标名称**     | **单位** |
|---------|-----------------|--------|
| sadd   | obkv_redis_sql_sadd_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191541",@LABELS}[@INTERVAL])) by (@GBLABELS)

### 采集 SQL

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```

## zadd

### 指标介绍

每秒处理 ZADD 语句数。

### 指标参数说明

| **指标项** |    **指标名称**     | **单位** |
|---------|-----------------|--------|
| zadd   | obkv_redis_sql_zadd_count | 次      |

### 计算表达式

sum(rate(ob_sysstat{stat_id="191557",@LABELS}[@INTERVAL])) by (@GBLABELS)

### 采集 SQL

* OceanBase V4.0 以下版本：

  ```sql
  
  ```

* OceanBase V4.0 及以上版本：

  ```sql
  
  ```
