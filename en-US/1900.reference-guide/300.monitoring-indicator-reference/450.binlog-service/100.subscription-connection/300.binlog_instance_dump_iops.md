|slug|binlog_instance_dump_iops|
|---|---|

# Network Traffic

This topic describes the subscription connection monitoring metric **<UI-TERM key="ocp-v2.Instance.Overview.Monitor.NetworkTraffic">Network Traffic</UI-TERM>** of a binlog instance. You can specify a statistical range to query the network traffic data of a binlog instance.

## Definition

The transmit traffic of a binlog instance.

## Description

| **<UI-TERM key="ocp-v2.Monitor.AddMonitor.VisualizationsTable.IndicatorItem">Metric</UI-TERM>** |   **Metric field name**    | **<UI-TERM key="ocp-v2.src.template.Table.Unit">Unit</UI-TERM>** |
|---------|---------------|--------|
| all     | binlog_instance_dump_iops | Byte      |

## Calculation expression

sum(binlog_instance_dump_iops{@LABELS}) by (@GBLABELS)
