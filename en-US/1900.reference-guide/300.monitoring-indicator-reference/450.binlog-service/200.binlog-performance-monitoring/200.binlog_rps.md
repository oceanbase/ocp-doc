|slug|binlog_rps|
|---|---|

# Binlog RPS

This topic describes the performance monitoring metric **Binlog RPS** of a binlog instance. This metric measures the read/write RPS of a binlog instance. You can specify a statistical range to query the read/write RPS of a binlog instance.

## read

### Definition

The read RPS of a binlog instance.

## Description

| **<UI-TERM key="ocp-v2.Monitor.AddMonitor.VisualizationsTable.IndicatorItem">Metric</UI-TERM>** |   **Metric field name**    | **<UI-TERM key="ocp-v2.Monitor.AddMonitor.AddMetricDrawer.Unit">Unit</UI-TERM>** |
|---------|---------------|--------|
| read     | binlog_instance_convert_fetch_rps | N/A      |

## Calculation expression

sum(binlog_instance_convert_fetch_rps{@LABELS}) by (@GBLABELS)

## write

### Definition

The write RPS of a binlog instance.

## Description

| **<UI-TERM key="ocp-v2.Monitor.AddMonitor.VisualizationsTable.IndicatorItem">Metric</UI-TERM>** |   **Metric field name**    | **<UI-TERM key="ocp-v2.src.template.Table.Unit">Unit</UI-TERM>** |
|---------|---------------|--------|
| write     | binlog_instance_convert_storage_rps | N/A      |

## Calculation expression

sum(binlog_instance_convert_storage_rps{@LABELS}) by (@GBLABELS)
