# I/O queue length

This topic describes the monitoring metric **I/O queue length** on a host.

## Definition

The I/O queue length.

## Description

| **<UI-TERM key="ocp-v2.Monitor.AddMonitor.VisualizationsTable.IndicatorItem">Metric</UI-TERM>** | **Metric name** | **<UI-TERM key="ocp-v2.Monitor.AddMonitor.MetricTable.Unit">Unit</UI-TERM>** |
|---------|---------------------|--------|
| io_qusize | io_qusize | N/A |

## Calculation expression

sum(delta(node_disk_io_time_weighted_seconds_total{@LABELS}[@INTERVAL])) by (@GBLABELS)
